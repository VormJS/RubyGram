<%= form_with(model: current_user.follower_relationships.build, local: true) do |f| %>
  <div><%= hidden_field_tag :following_id, @user.id %></div>
  <%= f.submit "Follow", class: "btn btn-primary follow_btn" %>
<% end %>